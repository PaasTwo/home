name: p2home

services:
  home:
    build: .
    image: paas2-home:latest
    container_name: paas2_home
    ports:
      - 6180:6180
    networks:
      - paas2-network
      - devmad
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.p2home.rule=Host(`p2.devmad.ir`)"
      - "traefik.http.routers.p2home.entrypoints=websecure"
      - "traefik.http.routers.p2home.tls=true"
      - "traefik.http.services.p2home.loadbalancer.server.port=6180"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:6180/"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  paas2-network:
    external: true
  devmad:
    external: true
